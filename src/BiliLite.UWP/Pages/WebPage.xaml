<local:BasePage
    x:Class="BiliLite.Pages.WebPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BiliLite.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
   
   
    Background="{ThemeResource TransparentBackground}">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Background="{ThemeResource NavigationViewTopPaneBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition  />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <AppBarButton x:Name="btnBack"  Width="48" Click="btnBack_Click">
                    <AppBarButton.Icon>
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE0A6;" />
                    </AppBarButton.Icon>
                </AppBarButton>
               <AppBarButton x:Name="btnForword"  Width="48" Click="btnForword_Click">
                    <AppBarButton.Icon>
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE0AD;" />
                      
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton x:Name="btnRefresh"  Width="48" Icon="Refresh" Click="btnRefresh_Click"></AppBarButton>
                
            </StackPanel>
            <AutoSuggestBox x:Name="UrlBox" PlaceholderText="地址栏" Visibility="Visible" Grid.Column="1" VerticalAlignment="Center" QueryIcon="Go"></AutoSuggestBox>
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <AppBarButton x:Name="btnInfo" Visibility="Visible"  Width="48" Icon="Emoji2" Margin="0 0"  Click="btnInfo_Click"></AppBarButton>
                <AppBarButton x:Name="btnShare" ToolTipService.ToolTip="复制网址" Width="48" Click="btnShare_Click">
                    <AppBarButton.Icon>
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE16F;" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton x:Name="btnOpenBrowser" Icon="World" ToolTipService.ToolTip="在浏览器打开" Width="48" Click="btnOpenBrowser_Click">
                   
                </AppBarButton>
            </StackPanel>
        </Grid>
        <controls:WebView2 Grid.Row="1" x:Name="webView" NavigationStarting="webView_NavigationStarting"  NavigationCompleted="webView_NavigationCompleted"></controls:WebView2>
    </Grid>
</local:BasePage>
